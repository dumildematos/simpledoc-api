server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    url: "jdbc:mysql://s15.infinitysrv.com:3306/simpledo_doc_db" #${BD_URL}
    username: "simpledo_adm" #${DB_USER}
    password: "}1$&{w6V}wPn" #${DB_PASS}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: false
  security:
    oauth2:
      client:
        registration:
          google:
            clientId: ${GOOGLE_CLIENT_ID}
            clientSecret: ${GOOGLE_CLIENT_ID}
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 200KB
      max-request-size: 200MB
file:
  upload-dir: /Documents/Pictures

mail:
  host: localhost
  port: 1025
  username: hello
  password: hello

  properties:
    mail:
      smtp:
        ssl:
          trust: "*"
        auth: true
        starttls:
          enable: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000
